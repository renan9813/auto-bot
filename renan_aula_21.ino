import processing.serial.*;

Serial myPort;
printArray(Serial.list());

import processing.serial.*;
import processing.sound

soundFile menuSong;
soundFile gameSong;

Serial myPort;

arrayList<integer>(), x = new ArrayList<Interger>(), y  = new Arraylist<Interger>();
int[] dx = {0, 0, 1, -1}, dy + {1, -1, 0, 0{;

int w = 30, h = 30, bs = 20, dir = 2;
int applex = 12, appley=10;
int newdir =0;

int score = 0;
int state = 0;
float s = 0;

PImage img1;
PImage img2;

boolean gameover = false
string instring;
